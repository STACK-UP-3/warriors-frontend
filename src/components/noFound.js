import React from 'react';

export default (props)=>{
return(
<div>
<h1>404: Can not Find The item {props.title}</h1>
      <p>
          The requested Item can not be found in our database.
      </p>
</div>
)};
